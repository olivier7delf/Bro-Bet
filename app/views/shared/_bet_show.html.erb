<div class="bet-infos">
  <h4><%= @bet.title %></h4>
  <p>
    <%= render 'shared/bet_show_result_input' %>
    <%= render 'shared/bet_show_resulted' %>
    <%= render 'shared/bet_show_result_pending' %>
    <% if Bet::PROGRESS[@progress.to_sym] < 2 %>
      <%= render 'shared/bet_show_joined' %>
    <% end %>
  </p>
  <p><%= @bet.description %></p>
  <p>Créateur : <%= @bet.user.nickname %></p>
  <p>Mise : <%= @bet.stake %></p>
  <p>Date de cloture : <%= @bet.closed_at.strftime("%d/%m/%Y à %kh%M") %></p>
  <p>Date de résultat : <%= @bet.resulted_at.strftime("%d/%m/%Y à %kh%M") %></p>
  <span>
    Ajouter un bouton pour voir nos paris puis en ajouter au tournoi un à la fois
  </span>
</div>
